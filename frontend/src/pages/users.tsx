import DefaultLayout from "@/layouts/default";

export default function UsersPage() {
  return (
    <DefaultLayout>
      <div className="p-6">
        <h1 className="text-2xl font-bold mb-4">จัดการผู้ใช้</h1>
        <p>เนื้อหาสำหรับการจัดการผู้ใช้ - เชื่อม backend users</p>
      </div>
    </DefaultLayout>
  );
}